# 测试与可观测性

> 📅 **更新时间**: 2026-06-29

---

## 目录

- [1. 测试策略](#1-测试策略)
- [2. 可观测性工程](#2-可观测性工程)
- [3. 实战工具](#3-实战工具)

---

## 1. 测试策略

### 1.1 测试金字塔

Agent 测试需要多层次策略：

```
        E2E 测试
       /         \
   集成测试     性能测试
     /    \      /    \
 单元测试  安全测试  负载测试
```

### 1.2 基准测试套件（完整代码）

```python
from typing import List, Dict
from dataclasses import dataclass
import time

@dataclass
class BenchmarkTask:
    """基准测试任务"""
    name: str
    input_data: str
    expected_output: str
    category: str
    difficulty: int = 1

class BenchmarkSuite:
    """基准测试套件"""
    
    def __init__(self):
        self.tasks: List[BenchmarkTask] = []
        self.results: List[dict] = []
    
    def add_task(self, task: BenchmarkTask):
        """添加测试任务"""
        self.tasks.append(task)
    
    def load_standard_benchmarks(self):
        """加载标准基准测试"""
        # 工具使用测试
        self.add_task(BenchmarkTask(
            name="calculator_basic",
            input_data="计算 256 * 128",
            expected_output="32768",
            category="tool_use",
            difficulty=1
        ))
        
        self.add_task(BenchmarkTask(
            name="search_fact",
            input_data="爱因斯坦哪一年获得诺贝尔奖？",
            expected_output="1921",
            category="tool_use",
            difficulty=2
        ))
        
        # 多步推理测试
        self.add_task(BenchmarkTask(
            name="multi_step_reasoning",
            input_data="如果 3 个人 3 天完成 3 个项目，9 个人 9 天完成多少？",
            expected_output="27",
            category="reasoning",
            difficulty=3
        ))
    
    def run_benchmark(self, agent) -> dict:
        """运行基准测试"""
        results = {
            "total_tasks": len(self.tasks),
            "passed": 0,
            "failed": 0,
            "by_category": {},
            "details": []
        }
        
        for task in self.tasks:
            start_time = time.time()
            
            try:
                # 执行任务
                result = agent.invoke({
                    "messages": [{"role": "user", "content": task.input_data}]
                })
                
                actual_output = result["messages"][-1]["content"]
                duration = time.time() - start_time
                
                # 评估
                passed = self._evaluate_output(
                    task.expected_output,
                    actual_output
                )
                
                if passed:
                    results["passed"] += 1
                else:
                    results["failed"] += 1
                
                # 按分类统计
                if task.category not in results["by_category"]:
                    results["by_category"][task.category] = {"passed": 0, "total": 0}
                results["by_category"][task.category]["total"] += 1
                if passed:
                    results["by_category"][task.category]["passed"] += 1
                
                results["details"].append({
                    "task": task.name,
                    "passed": passed,
                    "duration": duration
                })
                
            except Exception as e:
                results["failed"] += 1
                results["details"].append({
                    "task": task.name,
                    "passed": False,
                    "error": str(e)
                })
        
        return results
    
    def _evaluate_output(self, expected: str, actual: str) -> bool:
        """评估输出"""
        expected_keywords = set(expected.lower().split())
        actual_lower = actual.lower()
        
        # 如果包含所有关键词，认为通过
        return all(kw in actual_lower for kw in expected_keywords if len(kw) > 2)
    
    def generate_report(self, results: dict) -> str:
        """生成测试报告"""
        total = results["total_tasks"]
        passed = results["passed"]
        pass_rate = passed / total if total > 0 else 0
        
        report = f"""
基准测试报告
============

总览：
- 总任务数：{total}
- 通过：{passed}
- 失败：{results['failed']}
- 通过率：{pass_rate:.2%}

按分类：
"""
        
        for category, stats in results["by_category"].items():
            cat_pass_rate = stats["passed"] / stats["total"]
            report += f"- {category}: {stats['passed']}/{stats['total']} ({cat_pass_rate:.2%})\n"
        
        return report

# 使用
suite = BenchmarkSuite()
suite.load_standard_benchmarks()

# results = suite.run_benchmark(agent)
# report = suite.generate_report(results)
# print(report)
```

### 1.3 评估指标体系

```python
from dataclasses import dataclass

@dataclass
class AgentMetrics:
    """Agent 评估指标"""
    # 工具使用
    tool_accuracy: float = 0.0
    tool_selection_precision: float = 0.0
    
    # 任务完成
    task_completion_rate: float = 0.0
    average_iterations: float = 0.0
    
    # 输出质量
    output_accuracy: float = 0.0
    output_completeness: float = 0.0
    
    # 错误处理
    error_rate: float = 0.0
    recovery_success_rate: float = 0.0
    
    # 资源使用
    average_tokens: float = 0.0
    cost_per_task: float = 0.0
```

---

## 2. 可观测性工程

### 2.1 结构化日志（完整代码）

```python
import logging
import json
from pythonjsonlogger import jsonlogger

def setup_structured_logging():
    """设置结构化日志"""
    logger = logging.getLogger("agent")
    logger.setLevel(logging.INFO)
    
    handler = logging.StreamHandler()
    formatter = jsonlogger.JsonFormatter(
        '%(asctime)s %(name)s %(levelname)s %(message)s'
    )
    handler.setFormatter(formatter)
    logger.addHandler(handler)
    
    return logger

logger = setup_structured_logging()

# 使用
logger.info("Agent 启动", extra={
    "agent_name": "research_agent",
    "user_id": "user_123",
    "task": "搜索 AI 趋势"
})

logger.info("工具调用", extra={
    "tool_name": "search_web",
    "tool_input": {"query": "AI"},
    "duration_ms": 1200,
    "success": True
})
```

### 2.2 指标收集（完整代码）

```python
from prometheus_client import Counter, Histogram, Gauge

class AgentMetrics:
    """Agent 指标收集"""
    
    def __init__(self):
        # 请求指标
        self.requests_total = Counter(
            'agent_requests_total',
            'Total agent requests',
            ['agent_name', 'status']
        )
        
        # 延迟指标
        self.request_duration = Histogram(
            'agent_request_duration_seconds',
            'Agent request duration',
            ['agent_name'],
            buckets=[0.1, 0.5, 1.0, 2.0, 5.0, 10.0]
        )
        
        # Token 指标
        self.tokens_total = Counter(
            'agent_tokens_total',
            'Total tokens',
            ['type']
        )
        
        # 错误指标
        self.errors_total = Counter(
            'agent_errors_total',
            'Total errors',
            ['error_type']
        )
    
    def record_request(self, agent_name: str, status: str, duration: float):
        """记录请求"""
        self.requests_total.labels(agent_name, status).inc()
        self.request_duration.labels(agent_name).observe(duration)
    
    def record_tokens(self, token_count: int, token_type: str):
        """记录 Token"""
        self.tokens_total.labels(token_type).inc(token_count)
    
    def record_error(self, error_type: str):
        """记录错误"""
        self.errors_total.labels(error_type).inc()

# 全局指标实例
metrics = AgentMetrics()

# 使用
import time

start_time = time.time()
try:
    result = agent.invoke({"messages": [...]})
    duration = time.time() - start_time
    metrics.record_request("research_agent", "success", duration)
except Exception as e:
    duration = time.time() - start_time
    metrics.record_request("research_agent", "error", duration)
    metrics.record_error(type(e).__name__)
```

### 2.3 LangSmith 集成（完整代码）

```python
import os
from langsmith import Client, traceable

# 配置 LangSmith
os.environ["LANGCHAIN_TRACING_V2"] = "true"
os.environ["LANGCHAIN_API_KEY"] = "your-api-key"
os.environ["LANGCHAIN_PROJECT"] = "my-agent"

@traceable(run_type="chain")
def my_agent_chain(input: str) -> dict:
    """Agent 链（自动追踪）"""
    return agent.invoke({
        "messages": [{"role": "user", "content": input}]
    })

# 使用
result = my_agent_chain("解释量子计算")
# 在 LangSmith 中查看完整的执行轨迹
```

### 2.4 执行轨迹可视化（完整代码）

```python
from typing import List
import json
from datetime import datetime

class ExecutionTracer:
    """执行轨迹记录器"""
    
    def __init__(self):
        self.traces: List[dict] = []
        self.current_trace: List[dict] = []
    
    def start_trace(self, task: str):
        """开始追踪"""
        self.current_trace = [{
            "event": "start",
            "task": task,
            "timestamp": datetime.now().isoformat()
        }]
    
    def record_tool_call(self, tool_name: str, input_data: dict, output: str):
        """记录工具调用"""
        self.current_trace.append({
            "event": "tool_call",
            "tool": tool_name,
            "input": input_data,
            "output": output[:200],
            "timestamp": datetime.now().isoformat()
        })
    
    def record_llm_call(self, prompt_length: int, response: str, tokens: int):
        """记录 LLM 调用"""
        self.current_trace.append({
            "event": "llm_call",
            "prompt_length": prompt_length,
            "response": response[:200],
            "tokens": tokens,
            "timestamp": datetime.now().isoformat()
        })
    
    def end_trace(self, result: str):
        """结束追踪"""
        self.current_trace.append({
            "event": "end",
            "result": result,
            "timestamp": datetime.now().isoformat()
        })
        self.traces.append(self.current_trace.copy())
        self.current_trace = []
    
    def visualize_trace(self, trace_index: int = -1) -> str:
        """可视化轨迹"""
        trace = self.traces[trace_index]
        
        visualization = "执行轨迹：\n" + "=" * 60 + "\n"
        
        for event in trace:
            timestamp = event["timestamp"].split("T")[1].split(".")[0]
            
            if event["event"] == "start":
                visualization += f"[{timestamp}] 🚀 开始：{event['task']}\n"
            elif event["event"] == "tool_call":
                visualization += f"[{timestamp}] 🔧 工具：{event['tool']}\n"
            elif event["event"] == "llm_call":
                visualization += f"[{timestamp}] 🧠 LLM（{event['tokens']} tokens）\n"
            elif event["event"] == "end":
                visualization += f"[{timestamp}] ✅ 完成\n"
            
            visualization += "-" * 60 + "\n"
        
        return visualization

# 使用
tracer = ExecutionTracer()
tracer.start_trace("查询天气")
tracer.record_llm_call(500, "Thought: 查询天气", 100)
tracer.record_tool_call("weather_api", {"city": "北京"}, "晴天，25°C")
tracer.end_trace("完成")

print(tracer.visualize_trace())
```

---

## 3. 实战工具

### 3.1 LangSmith 集成

```python
import os

# 环境变量
os.environ["LANGCHAIN_TRACING_V2"] = "true"
os.environ["LANGCHAIN_API_KEY"] = "ls_xxx"
os.environ["LANGCHAIN_PROJECT"] = "production-agent"

# 自动追踪所有调用
from langsmith import traceable

@traceable
def agent_with_tracing(input: str):
    return agent.invoke({"messages": [{"role": "user", "content": input}]})
```

### 3.2 本地调试技巧

```python
# 逐步执行
snapshot = agent.invoke(
    {"messages": [{"role": "user", "content": "test"}]},
    {"configurable": {"thread_id": "debug_001"}}
)

# 查看状态
print(agent.get_state({"configurable": {"thread_id": "debug_001"}}))

# 修改状态后继续
agent.update_state(
    {"configurable": {"thread_id": "debug_001"}},
    {"messages": [{"role": "user", "content": "修改后的输入"}]}
)
```

### 3.3 性能分析工具

```python
import time
from functools import wraps
from collections import defaultdict

class PerformanceProfiler:
    """性能分析器"""
    
    def __init__(self):
        self.profiles = defaultdict(list)
    
    def profile(self, func_name: str):
        """性能分析装饰器"""
        def decorator(func):
            @wraps(func)
            def wrapper(*args, **kwargs):
                start_time = time.time()
                try:
                    result = func(*args, **kwargs)
                    elapsed = time.time() - start_time
                    
                    self.profiles[func_name].append({
                        "duration": elapsed,
                        "success": True
                    })
                    return result
                except Exception as e:
                    elapsed = time.time() - start_time
                    self.profiles[func_name].append({
                        "duration": elapsed,
                        "success": False,
                        "error": str(e)
                    })
                    raise
            return wrapper
        return decorator
    
    def get_stats(self, func_name: str) -> dict:
        """获取性能统计"""
        profiles = self.profiles[func_name]
        durations = [p["duration"] for p in profiles]
        
        import numpy as np
        return {
            "call_count": len(profiles),
            "avg_duration": float(np.mean(durations)),
            "p95_duration": float(np.percentile(durations, 95))
        }

# 使用
profiler = PerformanceProfiler()

@profiler.profile("llm_call")
def call_llm():
    return llm.invoke([...])

for _ in range(10):
    call_llm()

print(profiler.get_stats("llm_call"))
```
